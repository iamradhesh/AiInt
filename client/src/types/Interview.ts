export type InterviewSetup = {
  role: string;
  experience: number;
  techStack: string[];
};

export type InterviewReport = {
  score: number;
  feedback: string;
  strengths: string[];
  improvements: string[];
};